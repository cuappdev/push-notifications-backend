const {
  APN_AUTH_KEY_ID,
  APPLE_TEAM_ID,
  DB_HOST,
  DB_NAME,
  DB_PASSWORD,
  DB_PORT,
  DB_USERNAME,
  NODE_ENV,
} = process.env;

export {
  APN_AUTH_KEY_ID,
  APPLE_TEAM_ID,
  DB_HOST,
  DB_NAME,
  DB_PASSWORD,
  DB_PORT,
  DB_USERNAME,
  NODE_ENV,
};